# Where this app answers. Read at build time by the SPA and the extension
# (VITE_ prefix); the worker reads APP_URL from wrangler vars / .dev.vars.
# Set your own deployment's URL here — this default is the local dev server.
VITE_APP_URL=http://localhost:5173
